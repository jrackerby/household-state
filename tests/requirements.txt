# Test dependencies, DECLARED BESIDE THE SUITE (LAW §15).
#
# A list in .github/workflows/validate.yml is a second copy: it goes stale on
# the next import added, and its failure reads as a broken test rather than as
# a missing declaration. tempest_wx is the worked example — `import yaml` was
# green for months inside jrackerby/HA's shared .venv and went red at
# collection on that repo's first CI run (GH-670). CI installs from THIS file.
#
# Note what is NOT here: homeassistant. tests/ha_stubs.py supplies the slice of
# core the component imports, deliberately — see validate.yml's tests job.
pytest
PyYAML
# config_flow.py builds its schemas with voluptuous, so exercising the flow
# (the quality scale's config-flow-test-coverage) imports it for real.
voluptuous
# The quality scale's `test-coverage` rule wants above 95%. validate.yml fails
# the build below that, so the measurement is a gate rather than a number
# somebody re-derives by hand and writes into quality_scale.yaml.
pytest-cov
